# dboeke/homebrew-tap

Homebrew tap for [ash](https://github.com/dboeke/ash-cli), the agentic shell.

```sh
brew install dboeke/tap/ash
```

ash is built from source, so the binary is never quarantined and runs with no
"downloaded from the internet" warning.
